#include "crsf.h"

// CRC8-DVB-S2テーブル（多項式: 0xD5）
static const uint8_t crc8_dvb_s2_table[256] = {
    
};

uint8_t crsf_crc8(const uint8_t *data, size_t len) {
    if (data == NULL || len == 0) {
        return 0;
    }

    uint8_t crc = 0;
    for (size_t i = 0; i < len; i++) {
        crc = crc8_dvb_s2_table[crc ^ data[i]];
    }
    return crc;
}

uint16_t crsf_normalize_channel(int16_t input) {
    // 入力: -32768 〜 32767
    // 出力: 172 〜 1811
    //
    // 線形変換（四捨五入）: output = (input + 32768) * (1811 - 172) / 65535 + 172

    int32_t temp = (int32_t)input + 32768;  // 0 〜 65535
    // 四捨五入のため 65535/2 を加算してから除算
    temp = (temp * (CRSF_CHANNEL_MAX - CRSF_CHANNEL_MIN) + 32767) / 65535 + CRSF_CHANNEL_MIN;

    return (uint16_t)temp;
}

void crsf_pack_channels(const uint16_t channels[CRSF_NUM_CHANNELS], uint8_t *output) {
    // 16チャンネル × 11ビット = 176ビット = 22バイト
    // リトルエンディアンでパック

    // 出力をクリア
    for (int i = 0; i < CRSF_RC_CHANNELS_PACKED_PAYLOAD_SIZE; i++) {
        output[i] = 0;
    }

    // ビット位置を追跡
    int bit_pos = 0;

    for (int ch = 0; ch < CRSF_NUM_CHANNELS; ch++) {
        uint16_t value = channels[ch] & 0x7FF;  // 11ビットにマスク

        int byte_pos = bit_pos / 8;
        int bit_offset = bit_pos % 8;

        // 11ビットを書き込み（最大3バイトにまたがる可能性）
        output[byte_pos] |= (value << bit_offset) & 0xFF;

        if (bit_offset > 5) {
            // 2バイト目に続きがある
            output[byte_pos + 1] |= (value >> (8 - bit_offset)) & 0xFF;
        }
        if (bit_offset > 5) {
            // 3バイト目にも続きがある可能性
            if (bit_offset + 11 > 16) {
                output[byte_pos + 2] |= (value >> (16 - bit_offset)) & 0xFF;
            }
        } else if (bit_offset + 11 > 8) {
            // 2バイト目に続きがある
            output[byte_pos + 1] |= (value >> (8 - bit_offset)) & 0xFF;
        }

        bit_pos += 11;
    }
}

size_t crsf_build_rc_channels_packet(const uint16_t channels[CRSF_NUM_CHANNELS], uint8_t *buffer) {
    // パケット構造:
    // [0] SYNC (0xC8)
    // [1] LEN (24 = type + payload + crc)
    // [2] TYPE (0x16)
    // [3-24] PAYLOAD (22 bytes)
    // [25] CRC8

    buffer[0] = CRSF_SYNC_BYTE;
    buffer[1] = CRSF_RC_CHANNELS_PACKED_PAYLOAD_SIZE + 2;  // type + payload + crc = 24
    buffer[2] = CRSF_FRAMETYPE_RC_CHANNELS_PACKED;

    // ペイロードをパック
    crsf_pack_channels(channels, &buffer[3]);

    // CRC計算（type + payload）
    buffer[25] = crsf_crc8(&buffer[2], CRSF_RC_CHANNELS_PACKED_PAYLOAD_SIZE + 1);

    return 26;  // 全パケット長
}
